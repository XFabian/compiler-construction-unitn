
record Point {
	x, y : int
}

add(x : int, y : int) : int, int{
	return x + y, y
}
test() : int, int {

	return add(1,2)
}
x : int = 5
h : int // -1 with I64
e : bool // -1 with I32
createPoint(x : int, y : int) : Point {
	a : int[] = {1,2,3,}
	//a = {}
	//b : int[]
	c : bool[]
	l : int = length(a)
	d : int = a[1] + 3
	a[2] = 4
	x : Point = Point(5,10)
	return Point(1,2);
}
// cant return Point directly right now!
 doublePoint() : Point, Point {
 
 	return Point(1,2), Point(2,3)
 }
// 
swapP (p : Point) {
 	p.x, p.y = p.y, p.x;
 }
// 
main(): int {
	x : int, y : int = test()
 	p1 : Point, p2 : Point = doublePoint()
 	return p1.x
 }
