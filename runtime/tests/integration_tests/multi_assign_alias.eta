// EXPECT: 56
// Every right-hand side is evaluated before any assignment lands.
main() : int {
    a : int[] = {1, 2}
    a[0], a[1] = a[1], a[0]
    x : int = 2
    y : int = 3
    x, y = y, x + y
    return a[0] * 10 + a[1] + x * 10 + y
}
